# Use the official Nginx image
FROM nginx:alpine

# Copy the static HTML file into the Nginx server's web root
COPY index.html /usr/share/nginx/html/

# Expose port 80 to access the website
EXPOSE 80
